<!-- skyramp-pom-catalog: auto-generated by skyramp_reuse_code -->

# No Page Object Model layer in this repository

Every file the detector proposed was checked and none is a page object. The
repository has no `playwright.config.*`, no `tests/` page-object directory, and
no file containing `page.locator`, `getByRole`, `getByTestId`, or
`frameLocator`. Its own test suite is vitest + React Testing Library, which
renders components in-process and never drives a browser.

The detector matched on substrings inside React application source — the words
`switch`, `add`, `link`, `button`, and `Submit` appear as MUI component names,
JSX props, and TypeScript `switch` statements, not as Playwright selectors.

## Files checked (all rejected — application source, not page objects)

<!-- depth: signature -->

| File | Why it is not a page object |
|---|---|
| `src/core/lib/url.ts` | URL and header builder. `makeUrl`, `makeHeader`, `makeLink`, and the `WgerLink` enum. The matched `link` is `makeLink`; `switch` is a TypeScript switch over `WgerLink`. |
| `src/components/Routines/widgets/forms/BaseConfigForm.tsx` | React form component. Matched a MUI `Switch` and a Submit button label. |
| `src/components/Routines/widgets/forms/ProgressionForm.tsx` | React form component. Same MUI `Switch` / Submit match. |
| `src/components/Dashboard/EmptyCard.tsx` | React presentational card. Matched an "add" label and a MUI link. |
| `src/components/Exercises/screens/Add/state/exerciseSubmissionReducer.ts` | Reducer. Matched a TypeScript `switch` over action types. |
| `src/components/Routines/models/WorkoutSession.ts` | Domain model class. Matched a TypeScript `switch`. |
| `src/components/Routines/widgets/RoutineStatistics.tsx` | React chart widget. Matched a TypeScript `switch`. |
| `src/components/Routines/widgets/forms/RoutineForm.tsx` | React form component. Matched a Submit button label. |
| `src/components/Routines/widgets/forms/SessionLogsForm.tsx` | React form component. Matched a Submit button label. |
| `src/components/Exercises/forms/ImageModal.tsx` | React modal component. Matched a Submit button label. |
| `src/components/Exercises/models/exercise.ts` | Domain model class. Matched an "add" method name. |

## Result

Mapping table is empty — zero PREFERRED and zero HIGH matches. The generated
test keeps its inline locators. No `skyrampUtils.ts` was created either: it is
the only Skyramp-generated test in the repository, so there is no sibling test
to share a helper with.
