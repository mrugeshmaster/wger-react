<!-- skyramp-pom-catalog: auto-generated by skyramp_reuse_code -->

## trophiesTestData (module functions, not a class)
- **Import:** `@/tests/trophies/trophiesTestData`
- **Properties:**
  - `testTrophies` — selector: none (returns `Trophy[]` fixtures; the detector matched on the literal name `Beginner` inside this data, not on a page selector)
  - `responseTrophiesPage1` — selector: none (raw paginated API response fixture)
  - `responseTrophiesPage2` — selector: none (raw paginated API response fixture)
  - `responseUserTrophies` — selector: none (raw API response fixture)
  - `responseUserTrophyProgression` — selector: none (raw API response fixture)
- **Methods:**
  - `testTrophies()` — returns an array of `Trophy` model objects for in-process tests; selectors: none
  - `testUserTrophies()` — returns an array of `UserTrophy` model objects; selectors: none
  - `testUserProgressionTrophies()` — returns an array of `UserTrophyProgression` model objects; selectors: none

**Not a Page Object Model.** This module is a vitest/jsdom fixture factory. It exposes no Playwright
locators, no page handle, and no browser actions, so nothing in it can be called from a Playwright
spec. It was surfaced only because the string `Beginner` appears both in its fixture data and in the
recorded trace's assertion. Every action in the generated spec is therefore UNMAPPED against it.
