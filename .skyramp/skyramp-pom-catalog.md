<!-- skyramp-pom-catalog: auto-generated by skyramp_reuse_code -->

## src/pages/index.ts (module functions, not a class)
<!-- depth: signature -->
- **Import:** `@/pages`
- **Note:** NOT a Page Object Model. This is a React component barrel file — it
  re-exports page COMPONENTS (About, AddExercise, AddWeight, Calendar,
  CaloriesCalculator, Equipments, Ingredients, Login, Preferences, ApiPage,
  WeightOverview). It defines no classes, no locators and no Playwright actions,
  so nothing in it can be substituted into a browser test.
- **Properties:**
  - none — the detector matched the bare token `Overview` from the `WeightOverview`
    component export, not a locator.
- **Methods:**
  - none

## Repository POM status
- No Playwright Page Object Model exists in this repository.
- No `playwright.config.*` and no pre-existing Playwright specs to take
  conventions from; the repo's browser-free tests run under vitest.
- Consequence: generated UI specs keep their raw locators inline. Revisit only
  if a POM layer is added later.
